{
  "name": "@cindy/voice-protocol",
  "version": "0.0.0",
  "private": true,
  "description": "Cindy voice client/server wire contract: sessions, one-shot tickets and refinement payloads. Zero runtime deps",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
